<form method="post" action="{{ path('materiel_b_i_delete', {'id': materiel_b_i.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ materiel_b_i.id) }}">
    <button class="btn">Delete</button>
</form>
