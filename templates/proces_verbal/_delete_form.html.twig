<form method="post" action="{{ path('proces_verbal_delete', {'id': proces_verbal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ proces_verbal.id) }}">
    <button class="btn">Delete</button>
</form>
